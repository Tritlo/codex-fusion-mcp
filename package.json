{
  "name": "codex-fusion-mcp",
  "version": "0.1.0",
  "description": "MCP server that lets Claude consult Codex (via codex-acp) for review and co-planning — a lightweight fusion.",
  "type": "module",
  "bin": {
    "codex-fusion-mcp": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.21.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
