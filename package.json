{
  "name": "magi-council-mcp",
  "version": "0.1.0",
  "description": "MCP server that lets Claude convene a council of frontier models — Codex (codex-acp) and Grok (grok agent stdio) — for review, co-planning, and a three-way Magi deliberation.",
  "type": "module",
  "bin": {
    "magi-council-mcp": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.21.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
